version: v1.0
name: Build Studiobear Sites
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: Build Sites
    skip:
      when: branch != 'master' AND branch != 'develop'
    task:
      jobs:
        - name: Build Studiobear
          commands:
            - nvm use 10
            - checkout
            - cache restore monorepo-ci-$SEMAPHORE_GIT_BRANCH-$(checksum package.json)
            - yarn install
            - yarn build
            - yarn workspace my-site export
            - cache store monorepo-my-site-$SEMAPHORE_GIT_BRANCH-$(checksum package.json) apps/my-site/__sapper__/export
            - yarn workspace covid export
            - cache store monorepo-covid-$SEMAPHORE_GIT_BRANCH-$(checksum package.json) apps/covid/__sapper__/export
promotions:
  - name: Deploy Production My Site
    pipeline_file: studiobear-site-deploy-prod.yml
    auto_promote:
      when: result = 'passed' and branch = 'master'
  - name: Deploy Stage My Site
    pipeline_file: studiobear-site-deploy-stage.yml
    auto_promote:
      when: result = 'passed' and branch = 'develop'
  - name: Deploy Production Covid
    pipeline_file: studiobear-covid-deploy-prod.yml
    auto_promote:
      when: result = 'passed' and branch = 'master'
  - name: Deploy Stage Covid
    pipeline_file: studiobear-covid-deploy-stage.yml
    auto_promote:
      when: result = 'passed' and branch = 'develop'
